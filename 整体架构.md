# Fish Lab 整体架构

Fish Lab v0.4.2 是一个无依赖的浏览器单页游戏，核心流程是把文字、照片、真实模板或亲本基因统一转换成 `FishGene`，再由 SVG 渲染器和实时游戏循环生成可游动、可繁殖、可收藏的鱼。

## 总体数据流

```text
文字描述 ──► localParse() / Right Code Claude ──┐
鱼类照片 ──► Canvas 压缩 / Claude Vision ──────┤
真实模板 ──► realGene() ───────────────────────┤
选中母本 ──► deriveGene() ─────────────────────┼──► FishGene v2 ──► FISHES ──► fishSvg() + tick()
双亲基因 ──► crossGenes() ─────────────────────┘        │
                                                         └──► speciesKey() ──► CODEX 图鉴
```

## 技术与运行架构

- 项目由根目录单个 `index.html` 承载 HTML、CSS、数据、业务逻辑和 SVG 渲染，可直接本地打开或作为静态站部署到 GitHub Pages。
- 页面不依赖框架、构建工具、后端或外部资源包，所有游戏状态都在当前浏览器页面的内存中运行。
- `FishGene` 保存鱼的稳定身份、外观、谱系和来源，实体对象另外保存位置、速度、目标速度、朝向和 wander 计时等实时状态。
- `FISHES` 是鱼缸实体数组且最多容纳 12 条鱼，`CODEX` 是按物种键去重的内存图鉴。

## 生成与解析功能

- 离线文字孵化由 `localParse()` 识别颜色、胖瘦、尺寸、速度、眼数、花纹和特殊部件等关键词，其余字段使用受限随机值补全。
- 真实物种名会优先命中 10 个 `REAL_FISH` 手工模板，使小丑鱼、锦鲤、斗鱼等在离线模式下保持稳定特征。
- AI 文字孵化通过 Right Code 的 Anthropic 兼容端点调用 `claude-haiku-4-5-20251001`，将自然语言转换成受当前 Schema 约束的基因 JSON。
- AI 请求只发送运行时输入的 Right Code API Key，Key 只保存在页面内存并在刷新后清空。
- AI 调用失败时不会生成伪装成 AI 结果的鱼，而是显示具体错误并等待用户主动点击“改用离线解析生成”。
- 随机变异按钮当前直接调用离线解析器生成一条随机鱼，不依赖已选中的鱼或既有亲本。
- 照片转基因会先用 Canvas 把图片缩到最长边 1024px 并压成 JPEG 0.85，压缩后超过 4MB 才会拒绝上传。
- Claude Vision 只负责从照片提取 FishGene JSON，最终画面仍由本地 SVG 渲染器生成而不是由模型生成图片。

## 基因与物种模型

- 当前基因版本为 `schemaVersion: 2`，并用 `generatedBy: ai/local/bred/evolved` 标记 AI、离线、繁殖和定向衍生来源。
- 基因外观字段包括主辅颜色、体型、尾型、鳍型、嘴型、花纹、眼数、尺寸、胖瘦、眼睛大小、速度和特殊部件。
- `normalizeGene()` 负责白名单校验枚举、颜色、数值范围、数组长度和来源字段，无法满足核心字段的结果会被拒绝。
- `speciesKey()` 只使用体型、尾、鳍、嘴、花纹、眼数和特殊部件判断物种，不使用颜色、连续数值、谱系或生成来源。
- 当前 Schema 和 `fishSvg()` 只表达鱼类身体结构，因此章鱼、水母等非鱼生物即使被模型理解也只能被压缩成鱼形外观。

## 繁殖、遗传与衍生

- 配对模式支持依次点击两条鱼或先选中一条后 Shift+点击另一条，以双亲基因生成一个后代。
- `crossGenes()` 对离散字段随机二选一，对连续字段混合并加入近似高斯噪声，对颜色在 HSL 色相环上沿最短弧插值。
- 双亲共有部件的遗传概率为 90%、单方部件为 55%、新部件突变概率为 12%、尾型突变概率为 5%。
- 后代记录双亲 ID、代数、突变列表和 `generatedBy: bred`，并在双亲当前位置中间加入鱼缸。
- `deriveGene()` 从选中母本复制谱系、漂移 30–90° 色相、改变一到两个离散性状并增减特殊部件，结果标记为 `evolved`。
- 图鉴复育会复制已收录基因并分配新 ID，使同一物种可以重新加入鱼缸。

## 渲染与游戏循环

- `fishSvg()` 根据 FishGene 实时拼装鱼身、尾巴、鱼鳍、嘴、眼睛、花纹、特殊部件、渐变色和发光滤镜等 SVG 节点。
- `tick()` 使用帧率无关的 `dt` 更新 wander 目标、速度惯性、位置积分、碰壁反弹、朝向翻转和 DOM 位置。
- 运动状态出现非有限数值时实体会被重置到鱼缸中央，单帧异常也不会阻断下一次动画帧。
- 点击鱼会选中并打开基因观察台，点击空白水面取消选择，配对状态和选中状态通过描边与提示区反馈。
- 基因观察台显示编号、代数、来源、Schema、外观数值、部件和稀有度，并提供选配偶、导出 SVG 和放生操作。

## 稀有度与图鉴

- 稀有度分为普通、稀有、史诗和传说，评分综合特殊部件、突变、突变尾、眼数、代数和极端尺寸数值。
- 每次鱼进入鱼缸都会尝试写入 `CODEX`，同一物种键只保留稀有度更高的代表基因。
- 图鉴可导出包含 `schemaVersion: 2` 的 JSON，作为当前没有自动持久化时的手动备份方式。
- `migrateGeneV1toV2()` 可将 v0.3 图鉴的 `secondary/bodyShape/specialParts` 迁移为 v2 的 `accent/shape/parts`。
- 图鉴导入会先完整校验或迁移全部基因再写入，未知 Schema 和无效基因会明确报错而不是静默丢弃。

## 错误、安全与部署

- AI 错误会区分 401、429、网络失败和其他网关状态，错误横幅会显示 Right Code 返回的具体原因。
- 页面脚本错误和未处理的 Promise 错误会进入统一红色错误提示，同时游戏循环尽量保持继续运行。
- GitHub Pages 只提供静态文件且无法读取服务端环境变量，因此当前浏览器直连网关方案仅适合开发者本人知情测试。
- 正式公开推广前应改用后端代理保存 Token，或恢复官方端点并把第三方 Base URL 做成用户主动配置项。

## 当前尚未实现

- 鱼缸和图鉴刷新后仍会清空，因为 `localStorage` 正式本地存档尚未接入。
- 成长、饥饿、投喂和互动循环尚未进入实体状态或游戏循环。
- 非鱼类身体拓扑、音效音乐、完整美术、桌面封装、Steamworks 和商店发布流程尚未实现。
