# Fish Lab

一个语言驱动的程序化鱼类生成原型，也是“做出第一个鱼类游戏并上架 Steam”的起点。

当前版本是无依赖的单页 Demo：输入中文描述，离线解析颜色、体型、尺寸和游速，并把生成的鱼放进同一个鱼缸。它不调用外部 AI 服务，也不需要 API key。

## v0.3 功能

- 开场生成三条随机鱼，鱼缸上限为 12 条
- 孵化新鱼，或基于选中鱼进行随机变异
- 点击选中鱼并查看基因，点击空白水面取消选择
- 将选中的鱼导出为 SVG，或从鱼缸放生
- 使用实体数组和帧率无关的游戏循环处理 wander、惯性、运动与碰壁
- 配对模式依次选择双亲，或选中一条鱼后 Shift+点击另一条快速繁殖
- 离散基因随机继承，连续基因混合并加入高斯噪声，颜色在 HSL 空间沿最短弧插值
- 特殊部件有 8% 概率突变，尾巴有 5% 概率突变
- 普通、稀有、史诗、传说四级稀有度，以及野生/第 N 代谱系显示
- 物种图鉴按离散性状去重，可复育物种并导入/导出 JSON

## 玩法

点击“💕 配对繁殖”进入配对模式，再依次点击两条鱼；后代会在双亲中间诞生。也可以先选中一条鱼，再按住 Shift 点击另一条快速繁殖。

页面底部的物种图鉴会自动收录孵化过的物种。点击图鉴卡片可以复育该物种。图鉴当前只保存在内存中，刷新页面会清空，养出稀有物种后请先导出 JSON。

## 运行

直接用浏览器打开 `index.html`，或者在项目目录启动一个静态服务器：

```bash
python -m http.server 8000
```

然后访问 <http://localhost:8000>。

## 当前架构

```text
文本描述 ──► randomGene() ──────────────┐
双亲基因 ──► crossGenes() + mutation ───┼──► FishGene（身份与谱系）
                                          │          │
                                          │          ├──► speciesKey() ──► CODEX 图鉴
                                          ▼          │
                                    FISHES 实体数组   │
                                          │          │
                             tick(dt) ──► 运动状态 + DOM/SVG 渲染
```

原型暂时放在单个 `index.html` 中，便于直接部署到 GitHub Pages。每条实体将不变的 `gene` 与逐帧变化的运动状态分开保存。玩法确定后再拆分渲染、数据和游戏逻辑。

## 基因 Schema

```js
{
  id: "FL-023",
  name: "泡泡仙鱼",
  generation: 2,
  parents: ["FL-008", "FL-014"],
  mutations: ["独角"],
  color: "hsl(18.4 82.0% 61.0%)",
  accent: "#ff8c42",
  shape: "圆胖",
  tail: "扇尾",
  fin: "蝶鳍",
  mouth: "微笑",
  pattern: "斑点",
  eyeCount: 2,
  size: 1.0,
  fatness: 1.2,
  eyeSize: 1.0,
  speed: 1.0,
  parts: ["独角"]
}
```

## 路线图

- [x] 文本描述生成鱼类
- [x] 多条鱼同缸游动
- [x] 选中、查看基因、导出 SVG 与放生
- [x] 图鉴 JSON 导入与导出（网页原型临时存档）
- [x] 杂交、遗传与突变
- [x] 物种图鉴与稀有度
- [ ] 正式本地存档
- [ ] 成长、饥饿和互动循环
- [ ] 音效、音乐与完整美术风格
- [ ] 桌面版封装和 Steamworks 接入
- [ ] 商店页素材、测试与发布

## 部署

仓库根目录的 `index.html` 可直接作为 GitHub Pages 入口。在仓库 Settings → Pages 中选择从 `main` 分支部署即可。

## License

[MIT](LICENSE)
