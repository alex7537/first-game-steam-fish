# Fish Lab 变更记录

本文只记录已经落地到代码中的功能。尚未实现的计划仍以 README 路线图为准。

## v0.4.2

- Right Code 请求改用其模型列表中的完整 Haiku ID `claude-haiku-4-5-20251001`，并按 Curl 示例只发送 `x-api-key`。
- 兼容读取字符串或对象形式的网关错误正文，400 等错误会显示真实原因。
- API Key 弹窗和错误文案明确标注 Right Code，不再让个人测试误以为正在调用 Anthropic 官方端点。

## v0.4.1

- 孵化按钮根据 Key 配置显示“AI 孵化”或“离线孵化”，并标明 Claude/关键词模式。
- AI 调用失败时不再静默生成本地随机鱼；用户可在错误横幅中主动选择离线降级。
- 401、429 和网络连接失败分别给出明确错误与处理方向，成功提示以真实调用结果为准。
- FishGene 增加 `schemaVersion: 2` 与白名单来源字段 `generatedBy`，区分 AI、离线、繁殖和衍生。
- `generatedBy` 不参与 `speciesKey()`，真实鱼物种名的离线模板匹配保持不变。
- 默认通过 `https://right.codes/claude` 调用 `claude-haiku-4-5`，同时发送 Bearer Token 与 Anthropic 兼容请求头；Token 不写入源码。
- 照片不再按原始体积直接拒绝：先用 Canvas 缩到最长边 1024px，再按 JPEG 0.85 压缩，压缩后 4MB 为最终上限。
- 新增 `migrateGeneV1toV2()`，可迁移 v0.3 导出的 `version: 1` 图鉴；未知 Schema 或无效基因会明确报错。

## v0.4

- 新增真实鱼类馆，内置小丑鱼、斗鱼、锦鲤、鮟鱇、河豚、神仙鱼、金鱼、鲶鱼、狮子鱼和霓虹灯鱼 10 个母本模板。
- 为真实模板补充可见特征，包括丝带尾、帆状背鳍、吻须、诱饵、发光、獠牙、尖刺、红腹和微光。
- 离线解析支持通过真实鱼物种名直接命中对应模板。
- 新增“衍生新物种”：从选中鱼继承谱系，强制漂移 30–90° 色相、改变 1–2 个离散性状，并增加或移除特殊部件。
- 新增照片转基因：通过 Claude Vision 将 JPEG、PNG、GIF 或 WebP 鱼类照片转换为现有基因结构。
- 照片上传限制为 7 MB；API Key 仅保存在页面内存中。
- 文字解析和图片解析共用 `GENE_SCHEMA_TEXT`。
- 复用现有 FishGene、鱼缸实体和图鉴流程，没有引入新依赖。

## v0.3.1

- 游戏循环改为异常后继续请求下一帧，并增加全局错误提示。
- 增加实体数值异常防护，异常鱼会重置到鱼缸中央。
- 将位移与鱼体朝向翻转分离，并增加平滑转向和速度死区。
- 明确区分离线解析与 Claude API 解析状态。
- 调整繁殖遗传：双亲共有部件 90%、单方部件 55%、新部件突变 12%、尾巴突变 5%。

## v0.3

- 新增配对模式和 Shift+点击快速繁殖。
- 新增离散基因继承、连续基因混合、高斯噪声与 HSL 色相混合。
- 新增普通、稀有、史诗、传说四级稀有度和代数谱系。
- 新增物种图鉴、物种复育以及图鉴 JSON 导入/导出。

## v0.2

- 从单鱼全局状态改为 `FISHES` 实体数组。
- 新增三条开场鱼、12 条鱼容量限制、选中、查看基因、放生和 SVG 导出。
- 新增帧率无关的游戏循环、wander、惯性、碰壁反弹和游动方向翻转。

## v0.1

- 建立无依赖单页原型。
- 根据文字描述生成程序化鱼类 SVG。
- 建立初始 FishGene 数据结构和本地关键词解析。
